Gaussian downsampling of an image with ImageJ on-board tools.

==Motivation==

Sound downsampling of an image requires the elimination of image frequencies higher than half the sampling frequency in the result image (see the [http://en.wikipedia.org/wiki/Nyquist%E2%80%93Shannon_sampling_theorem Nyquist–Shannon sampling theorem]).  The exclusive tool for this is [http://en.wikipedia.org/wiki/Gaussian_function Gaussian convolution].

==Download==

Get a snapshot from the git repository here: [http://pacific.mpi-cbg.de/cgi-bin/gitweb.cgi?p=fiji.git;a=blob_plain;f=plugins/Examples/downsample_.js downsample_.js].

==Documentation==

This script calculates the required Gaussian kernel for a given target '''width''' or '''height''', smooths the image and resamples it.  The target size must be smaller than the source image size.

Furthermore, you can define the "intrinsic" Gaussian kernel of the source and target images.  An optimal sampler is identified by sigma=0.5.  If your source image was blurred already, you may set a higher '''source sigma''' for a sharper result.  Setting '''target sigma''' to values smaller than 0.5 makes the result appear sharper and therefore eventually aliased.

==Example==

A picture is worth a thousand words, so here is an example.  You see a 2,048&times;2,048px transmission electron micrograph downsampled to 100&times;100px.  For better illustration, the examples are shown at 200%.

{|
|style="vertical-align:top" |[[Image:Downsample-imagej.png|none|frame|ImageJ interpolated scaling]]
|style="vertical-align:top" |[[Image:Downsample-ts-0.25.png|none|frame|Gaussian downsampling with target sigma=0.25]]
|style="vertical-align:top" |[[Image:Downsample-ts-0.5.png|none|frame|Gaussian downsampling with target sigma=0.5]]
|}

==See also==

* [[Javascript Scripting]]
* [[Scripting Help]]
* [[Scripting_comparisons]]

[[Category:Scripting]]
