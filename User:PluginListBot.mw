Everytime the continuous release is updated (triggered by a change on the update sites), the [[Template:PluginList|list of plugins]] is updated. To mark this as an automatic update, this update is performed via the account named ''PluginListBot''.
